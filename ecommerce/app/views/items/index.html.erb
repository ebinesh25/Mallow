<p style="color: green"><%= notice %></p>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", [@hotel,item] %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_hotel_item_path %>
<br><br>
<%= link_to "back to hotel", @hotel %>